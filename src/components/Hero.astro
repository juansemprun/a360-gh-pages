---
import { render, type CollectionEntry } from 'astro:content';

// interface HeroLandingProps {
//   heroEntry: CollectionEntry<'hero'>;
// }

// interface HeroLandingProps {
//   heroEntry: CollectionEntry<'pages'>;
// }

// const { heroEntry } = Astro.props as HeroLandingProps;

// const { Content } = await render(heroEntry);
---

<section
  class="bg-gradient-primary-darker min-h-screen w-full overflow-hidden pt-16"
  data-theme="light"
>
  <!-- Main content container -->
  <div class="container-custom relative z-10 flex flex-col items-center gap-12 py-20 lg:py-32">
    <!-- <Content /> -->
    <slot />
  </div>
</section>
<!-- Bottom gradient line -->
<!-- <div
  class="bottom-0 h-0.5 w-full bg-gradient-to-r from-[#F771BF] via-[#f04d66] to-[#fd7043] shadow-lg"
>
</div> -->
